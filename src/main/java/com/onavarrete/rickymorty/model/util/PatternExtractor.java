package com.onavarrete.rickymorty.model.util;

public interface PatternExtractor {
	
	Integer getLocationIdFromUrl(String url);
}

/***
* re denominación de interfaces y métodos para proveer sentido más específico y hacer que sean entendibles de forma más expedita,
* proveer mayor información sobre contexto de la implementación a través de la clase que implementa interface
***/ 